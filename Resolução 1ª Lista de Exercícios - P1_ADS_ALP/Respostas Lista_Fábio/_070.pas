Program Pzim ;
uses crt;
var
	n,k,nimpar:integer;
Begin
	writeln('Escreva quantos Números Primeiros Ímpares quer ver:');
	readln(n);
	for k:= 1 to n do
	begin
		if (k>1) then nimpar:=K+(K-1)
		else nimpar:=k; 
		write(nimpar:5);
	end;
readln;  
End.